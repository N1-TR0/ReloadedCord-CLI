# ReloadedCord-CLI
**ReloadedCord CLI**  A lightweight, TUI-based Python 3.6+ based utility for macOS that automates the downloading and patching of Discord to bypass "Host Update" loops on legacy OS versions (10.9 – 10.15). Features auto-version detection, hardware model identification, and a native macOS save dialog.
